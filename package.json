{
  "name": "exibition",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && rm -rf dist/images && cp -r images dist/images"
  },
  "devDependencies": {
    "vite": "latest"
  }
}